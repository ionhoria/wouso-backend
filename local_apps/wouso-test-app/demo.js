const { Session, Question } = require('./models/session_question')
const { Answer } = require('./models/answer')

const doShit = async () => {
  // const session = await Session.findById(1)
  // const question = await Question.findById(18)
  // session.addQuestion(question)
}

module.exports = doShit
